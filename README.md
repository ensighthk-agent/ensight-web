# Ensight HK Website

Simple bilingual static website for Ensight HK.

## Local Preview

Open `index.html` directly in a browser, or serve the folder with any static server.

## Deployment

This project is ready for Cloudflare Pages with no build command and `/` as the output directory.
